const fahrenheitToKelvinOrKelvinTofahrenheit = temp => !/[fk]/i.test(temp[temp.length - 1]) || /[fk]/i.test(temp[0]) || temp[0] === ' ' ? new Error('invalid input; the valid format is #F (or #f) or #K (or #k).') : /f/i.test(temp) ? `${(temp.replace(/f/i, '') -32) / 1.8 + 273.15} °K` : `${1.8 * (temp.replace(/k/i, '') - 273.15) + 32} °F`;
